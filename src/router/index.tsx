import React from 'react';
import { HashRouter, Switch} from  'react-router-dom';

const BasicRoute = () => (
    <HashRouter>
        <Switch>
        </Switch>
    </HashRouter>
);

export default BasicRoute;